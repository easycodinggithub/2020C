#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>

struct Point { // 구조체 점
	int x, y;
};

struct Student {
	char name[20];
	char stu_num[10];
	int age;
};

int main() {
	/*struct Point p = { 4, 5 };
	printf("x = %d, y = %d \n", p.x, p.y);
	struct Point p1;
	p1.x = 5;
	p1.y = 8;
	printf("x = %d, y = %d \n", p1.x, p1.y);
	struct Point myPoint[20];
	myPoint[0].x = 1;
	myPoint[0].y = 1;
	printf("x = %d, y = %d \n", myPoint[0].x, myPoint[0].y);
	myPoint[1].x = 100;
	myPoint[1].y = 100;
	printf("x = %d, y = %d \n", myPoint[1].x, myPoint[1].y);
	printf("x = %d, y = %d \n", myPoint[0].x, myPoint[0].y);*/

	struct Student class11[20];
	return 0;
}